Set the ImageUpdate to off
If ImageFound(text:"Creating Your Plan") then
	LogSuccess "Page validated"
else If ImageFound(text:"Building the Plan") then
	LogSuccess "Page validated"
Else
	LogError "Page did not validated"
	
End if
