Set the ImageUpdate to off
If ImageFound(text:"Getting married") then
	LogSuccess "Page validated"
else If ImageFound(text:"The money Talk") then
	LogSuccess "Page validated"
Else
	LogError "page did not validate"
	
End if
